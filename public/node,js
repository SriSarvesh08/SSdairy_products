const nodemailer = require('nodemailer');

let transporter = nodemailer.createTransport({
  service: 'gmail',
  auth: {
    user: 'your@gmail.com',
    pass: 'your-app-password',
  }
});

let mailOptions = {
  from: 'your@gmail.com',
  to: email,
  subject: 'Mala Ghee Order Confirmation',
  text: `Hi ${name}, your order of ₹${amount} has been received and will be shipped to:\n${address}`
};
window.location.href = "thankyou.html";
